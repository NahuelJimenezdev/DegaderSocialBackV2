services:
  backend:
    build: .
    restart: always
    env_file: .env
    environment:
      - REDIS_HOST=redis
      - REDIS_PASSWORD=${REDIS_PASSWORD}
    ports:
      - "3001:3001"
    volumes:
      - uploads_data:/usr/src/app/uploads
    depends_on:
      - redis
    dns:
      - 8.8.8.8
      - 8.8.4.4
    extra_hosts:
      - "cluster0.pcisms7.mongodb.net:89.192.9.237"
      - "ac-3ts5dmr-shard-00-00.pcisms7.mongodb.net:89.192.9.237"
      - "ac-3ts5dmr-shard-00-01.pcisms7.mongodb.net:89.192.9.252"
      - "ac-3ts5dmr-shard-00-02.pcisms7.mongodb.net:89.192.10.9"
      - "cluster0-shard-00-00.pcisms7.mongodb.net:89.192.9.237"
      - "cluster0-shard-00-01.pcisms7.mongodb.net:89.192.9.252"
      - "cluster0-shard-00-02.pcisms7.mongodb.net:89.192.10.9"
    networks:
      - degader-network

  redis:
    image: redis:alpine
    restart: always
    command: redis-server --requirepass "${REDIS_PASSWORD}" --appendonly yes
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - degader-network

  frontend:
    build: 
      context: ../DegaderSocialFrontV2
      dockerfile: Dockerfile
      args:
        - VITE_API_URL=https://degadersocial.com/api
        - VITE_ENV=production
        - VITE_FIREBASE_API_KEY=${VITE_FIREBASE_API_KEY}
        - VITE_FIREBASE_AUTH_DOMAIN=${VITE_FIREBASE_AUTH_DOMAIN}
        - VITE_FIREBASE_PROJECT_ID=${VITE_FIREBASE_PROJECT_ID}
        - VITE_FIREBASE_STORAGE_BUCKET=${VITE_FIREBASE_STORAGE_BUCKET}
        - VITE_FIREBASE_MESSAGING_SENDER_ID=${VITE_FIREBASE_MESSAGING_SENDER_ID}
        - VITE_FIREBASE_APP_ID=${VITE_FIREBASE_APP_ID}
        - VITE_FIREBASE_VAPID_KEY=${VITE_FIREBASE_VAPID_KEY}
    restart: always
    ports:
      - "8080:80"
    networks:
      - degader-network

networks:
  degader-network:
    driver: bridge

volumes:
  uploads_data:
  redis_data:
